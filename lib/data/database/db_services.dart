import 'package:flutter/material.dart';

class DbServices extends StatelessWidget {
  const DbServices({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
